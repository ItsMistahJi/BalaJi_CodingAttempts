#some tips on the project
#To install django Python 
python -m pip install Django

#to start a project
django-admin startproject <name of folder>

#run server
python manage.py runserver

#to create and start on creating an application
python manage.py startapp <name of your application>

#to reflect all changes carried out in application folder
python manage.py migrate
